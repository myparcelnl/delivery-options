name: 'Run tests 🧪'

on:
  pull_request:
    branches:
      - main
      - alpha
      - beta

  merge_group:

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
  test:
    uses: ./.github/workflows/--run-tests.yml
    secrets: inherit
